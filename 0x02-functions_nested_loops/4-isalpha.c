#include "main.h"
/**
 * _isalpha - checks for alphabets
 * @c: character
 * Return: 1 if character is alphabet, 0 if otherwise
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
		return (0);
	else
		return (0);
}
