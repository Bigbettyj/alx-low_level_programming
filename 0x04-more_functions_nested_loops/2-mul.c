#include "main.h"
/**
 * mul - will multiple two integers and print the result
 * @a: the first integer
 * @b: the second integer
 * Return: Always 0 (success)
 */
int mul(int a, int b)
{
	return (a * b);
}
